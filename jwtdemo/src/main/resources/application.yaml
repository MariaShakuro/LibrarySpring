spring:
  datasource:
    url: jdbc:mysql://mysql:3306/security_db?createDatabaseIfNotExist=true
    username : root
    password : dsova2207_12
    diver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
     ddl-auto: update
    show-sql: true
  test:
   database:
    replace: none

server:
  port: 8083